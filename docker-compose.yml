x-runtime-classification:
  status: legacy_blocked
  reason: unsupported_root_compose_use_infra_and_kraken_spot_launchers

services:
  legacy-root-compose-blocked:
    image: busybox:1.36
    command:
      - /bin/sh
      - -c
      - >
        echo 'unsupported legacy root compose; use infra/docker-compose.yml for infra only and scripts/run_kraken_spot_*.sh for runtime';
        exit 64
    profiles:
      - legacy-blocked

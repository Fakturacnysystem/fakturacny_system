#!/usr/bin/env bash
set -euo pipefail

require_env() {
  local name="$1"
  if [[ -z "${!name:-}" ]]; then
    echo "Missing required env var: ${name}" >&2
    exit 1
  fi
}

run_robot() {
  PYTHONPATH=src python3 -m autonomous_investment_robot "$@"
}
